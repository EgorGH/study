program answer_simple;

var
  n, m, x: longint;
  a: array[1..Lim] of longint;

  procedure f(level, boxes: longint);

  function full_search(x: longint): longint;
  begin
    d := f(n, a[n]);
  end;

begin
  readln(n, m);

  for i := 1 to n do
    read(a[i]);
  readln();

  for i := 1 to m do
  begin
    read(x);
    writeln(full_search(x));
  end;
  readln();

  readln();
end.

